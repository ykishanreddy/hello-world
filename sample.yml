apiVersion: apps/v1
kind: Pod
metadata:
  name: nodehelloworld.example.com
  labels:
    app: helloworld
spec:
  containers:
  - name: k8s-demo
    image: httpd
    ports:
    - name: nodejs-port
      containerPort: 3000

